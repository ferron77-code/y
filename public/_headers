# Bundled assets carry a content hash, so they can be cached forever.
/assets/*
  Cache-Control: public, max-age=31536000, immutable

# The films sit at fixed paths, so keep revalidation cheap but honest —
# a re-encode reaches visitors instead of being served stale.
/world/*
  Cache-Control: public, max-age=3600, must-revalidate
